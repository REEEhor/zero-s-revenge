#pragma once


/// @brief Enum for directions in which the CMovableObject can move.
namespace Direction {
    enum EDirection {
        NONE = 0,
        UP,
        DOWN,
        LEFT,
        RIGHT,
        DIRECTION_COUNT
    };
}